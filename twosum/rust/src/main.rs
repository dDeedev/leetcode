fn main() {
    let tc1: Vec<i32> = Vec::from([2, 7, 11, 15]);
    let tc2: Vec<i32> = Vec::from([3, 2, 4]);
    let tc3: Vec<i32> = Vec::from([3, 3]);
}

fn two_sum(nums: Vec<i32>, target: i32) -> Vec<i32> {
    vec![]
}
